🚀 Các Bước Cài Đặt Flask trên VS Code
1. Cài đặt Python Extension cho VS Code
Đây là bước quan trọng để VS Code có thể hỗ trợ tốt nhất cho lập trình Python.

B1 : Mở VS Code.

- Nhấn tổ hợp phím Ctrl+Shift+X (hoặc Cmd+Shift+X trên macOS) để mở Extensions (Tiện ích mở rộng).

- Tìm kiếm "Python" và cài đặt extension của Microsoft.

B2 : Tạo Thư mục Dự án và Môi trường Ảo (Virtual Environment)
Sử dụng môi trường ảo giúp cô lập các thư viện Flask khỏi hệ thống Python chung, tránh xung đột giữa các dự án.

- Mở Terminal trong VS Code (View > Terminal hoặc Ctrl+`).

- Tạo một thư mục dự án mới và di chuyển vào đó (ví dụ: mkdir my_flask_app sau đó cd my_flask_app).

- Tạo môi trường ảo bằng lệnh:

+ Bash
+ python -m venv venv 

B3. Kích hoạt Môi trường Ảo
- Trên Windows:

Bash
.\venv\Scripts\activate
Trên macOS hoặc Linux:

Bash
source venv/bin/activate
Sau khi kích hoạt, bạn sẽ thấy tên môi trường ảo (venv) xuất hiện ở đầu dòng lệnh Terminal.

B4. Cài đặt Flask
Khi môi trường ảo đã được kích hoạt, bạn sử dụng lệnh pip để cài đặt Flask vào môi trường đó.

Chạy lệnh:

Bash
pip install Flask
Bạn cũng có thể kiểm tra xem Flask đã được cài đặt thành công chưa bằng lệnh pip show Flask.

B5. Tạo Ứng dụng Flask Đơn giản
Tạo một file mới trong thư mục dự án, ví dụ app.py.

Thêm đoạn code Flask cơ bản sau:

Python
from flask import Flask

app = Flask(__name__)

@app.route('/')
def hello_world():
    return 'Hello, World!'

if __name__ == '__main__':
    app.run(debug=True)
6. Chạy Ứng dụng Flask
Trong Terminal (đã kích hoạt venv), chạy ứng dụng bằng lệnh:

Bash
python app.py



